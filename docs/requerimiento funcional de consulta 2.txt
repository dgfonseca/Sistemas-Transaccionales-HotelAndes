SELECT ID, COUNT(ID)
FROM SIRVEN SIRV
INNER JOIN SERVICIOS SERV ON SERV.ID=SIRV.IDSERVICIO
INNER JOIN SERVICIOS_CONVENCION SERVCO ON SERV.ID=SERVCO.ID_SERVICIO
WHERE ROWNUM<=20
GROUP BY ID
ORDER BY COUNT(ID) DESC;